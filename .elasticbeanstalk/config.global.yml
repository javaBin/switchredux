branch-defaults:
  master:
    environment: switchredux-prod
global:
  application_name: switchredux
  default_ec2_keyname: javabin
  default_region: eu-central-1
  profile: javabin
  sc: git
deploy:
  artifact: app.zip